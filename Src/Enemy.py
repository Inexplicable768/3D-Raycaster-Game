import pygame as pg
import numpy as np

class Enemy:
    def __init__(self, name: str, pos: pg.Vector2):
        self.x = pos.x,
        self.y = pos.y
        self.name = name
    def move():
        pass
    #if the terrain height is ever more than the enemy move the enemy up